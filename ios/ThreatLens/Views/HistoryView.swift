import SwiftUI

struct HistoryView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("History placeholder")
            }
            .navigationTitle("History")
        }
    }
}
