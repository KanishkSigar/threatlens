import SwiftUI

struct ScannerView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Scanner placeholder")
            }
            .navigationTitle("Scan")
        }
    }
}
